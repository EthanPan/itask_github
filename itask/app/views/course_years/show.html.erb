

<%= render "layouts/course_year_menu"%>
<%= @course_year.notice %>
<br/>

<% if user_signed_in?  %>
   <%= link_to '申请加入该课程',apply_course_year_path(@course_year),:class => 'btn' if !@course_year.has_a_student(current_user.id)&& cannot?(:mange ,@course_year)%>
<% end %>
<%= link_to '更新公告', edit_course_year_path(@course_year) ,class:"btn" if can? :manage,@course_year%> 