<h2><%= @assignment.title %></h2>
<p><%= @assignment.desp %></p>

<table  class="table table-hover table-striped">                        
	<thead>
                <tr>
                  <th>学生</th>
                  <th>上交时间</th>
                  <th>附件名称</th>
                  <th>操作</th> 

                </tr>
  </thead> 
  <tbody> 
	<% @assignment.student_course_assignments.each do |sta| %>     
	<tr>  
		<td><% if !sta.blank? %>
			<% if !sta.user.blank?%>
			    <%= sta.user.name %>
			    <%end%>
		   <%end%>
		</td> 
		<td><%=sta.created_at%></td> 
		<th></th>
		<td></td> 
	</tr> 
	<%end%>   
	</tbody>
</table>
<%= link_to 'Create new', new_assignment_student_course_assignment_path(@assignment) %>
